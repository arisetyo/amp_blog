/**
 * Adding a non-Flow component in the app
 * @author Arie M. Prasetyo
 * @copyright 2021
 */

import React from 'react';

/**
 * A simple component without static typing
 */
const NoFlowComponent = () => (
  <div>
    <h2>Dynamic typing</h2>
    <em>But sometimes you just want to be dynamic and live on the edge.</em>
  </div>
);

export default NoFlowComponent;